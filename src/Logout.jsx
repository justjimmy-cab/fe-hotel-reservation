import { Button } from '@mui/material';


const Logout = () => {
    return (
        <Button variant='contained' sx={{marginRight: '-85%'}}>
            Logout
        </Button>
    );
}

export default Logout;
